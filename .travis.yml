language: node_js
dist: trusty
node_js:
  - v8
  - v9
script:
  - npm run check
  - npm run test
after_script:
  - npm install coveralls -- 'cat ./coverage/lcov.info | coveralls'
  - npm install codecov -- 'cat ./coverage/lcov.info | codecov'
